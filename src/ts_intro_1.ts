console.log("Hellow TypeScript!");

/*
 * Since we're using typescript without a framework, to transpile a typescript file into a specific directory, use the following command:
 * [Skeleton]     tsc file.ts --outDir /PATH/
 * [Original]     tsc src/ts_intro_1.ts --outDir ../bin/
 */
